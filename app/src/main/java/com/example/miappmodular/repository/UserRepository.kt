package com.example.miappmodular.repository

/**
 * ARCHIVO TEMPORALMENTE DESHABILITADO
 *
 * Este UserRepository usaba Room (base de datos local) que fue eliminado cuando
 * migramos al backend de SaborLocal.
 *
 * Para usar autenticación, usar AuthSaborLocalRepository en su lugar.
 *
 * TODO: Implementar nuevo UserRepository que use AuthSaborLocalRepository y
 * el endpoint /api/auth/profile del backend de SaborLocal.
 */

/*
import android.content.Context
import com.example.miappmodular.data.local.SessionManager
import com.example.miappmodular.data.remote.dto.LoginRequest
import com.example.miappmodular.data.remote.dto.SignUpRequest
import com.example.miappmodular.data.remote.RetrofitClient
import com.example.miappmodular.data.remote.SaborLocalApiService
import com.example.miappmodular.data.remote.dto.ClienteDto
import kotlinx.coroutines.Dispatchers
import kotlinx.coroutines.flow.Flow
import kotlinx.coroutines.flow.firstOrNull
import kotlinx.coroutines.withContext
import java.security.MessageDigest
import java.util.Date

class UserRepository(context: Context) {
    // Código comentado - ver AuthSaborLocalRepository
}
*/
